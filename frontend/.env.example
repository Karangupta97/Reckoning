# Mapbox GL JS access token (public token, starts with pk.).
# Powers the Predictive Analytics mini map on the landing page.
# Create one at https://account.mapbox.com/access-tokens/.
# When empty, the card gracefully falls back to a static gradient heatmap.
NEXT_PUBLIC_MAPBOX_TOKEN=
