# Mock fallback is always OFF in production.
# Both this flag and the team email gate must be true for mock to activate,
# so setting this to false is a hard off — safe even if an email slips through.
NEXT_PUBLIC_ENABLE_MOCK=false
